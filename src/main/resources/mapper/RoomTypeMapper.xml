<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pcx.hotel_admin.mapper.RoomTypeMapper">

    <resultMap id="BaseResultMap" type="com.pcx.hotel_admin.entity.RoomType">
        <id column="id" property="id"/>
        <result column="type_name" property="typeName"/>
        <result column="price" property="price"/>
        <result column="capacity" property="capacity"/>
        <result column="description" property="description"/>
        <result column="active" property="active"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM room_type WHERE active = 1
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM room_type WHERE id = #{id} AND active = 1
    </select>

    <insert id="insert" parameterType="com.pcx.hotel_admin.entity.RoomType" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO room_type (type_name, price, capacity, description, active, create_time, update_time)
        VALUES (#{typeName}, #{price}, #{capacity}, #{description}, 1, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.pcx.hotel_admin.entity.RoomType">
        UPDATE room_type
        SET type_name = #{typeName},
            price = #{price},
            capacity = #{capacity},
            description = #{description},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE room_type SET active = 0, update_time = NOW() WHERE id = #{id}
    </update>

</mapper>
